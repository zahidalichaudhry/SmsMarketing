<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thinkgeniux.sms_marketing">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fakhar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fakhar"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Bulk_Message"
            android:label="@string/title_activity_bulk__message"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Sms"
            android:label="@string/title_activity_sms"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Reports"
            android:label="@string/title_activity_reports"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Groups"
            android:label="@string/title_activity_groups"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>